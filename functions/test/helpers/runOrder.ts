import "../callable/auth";
import "../callable/courses";
import "../callable/misc/sendPlatformFeedback";
import "../callable/quizzes";
import "../callable/reports";

import "../triggers/auth";
import "../triggers/database";

//
// Add an import for all test files (except setup/cleanup) here in the desired order
// This can control the execution order of the tests and ensure setup and tear down are run first and last, respectively
//
